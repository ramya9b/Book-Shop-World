node_modules/
.DS_Store
*.log
.vercel/

# test artifacts (generated)
e2e/node_modules/
e2e/test-results/
e2e/playwright-report/
e2e/.cache/
media/ppt/*.pptx
media/songs/*.mp4

# Story-book masters: print-resolution PDFs, 350-410 MB each (~2.2 GB total).
# Far over GitHub's 100 MB per-file limit, and never served. Only the
# web-sized previews under media/books/web/ are committed.
media/books/*.pdf
media/books/preview/
# Canva re-exports of the same masters (~10 MB each, also never served).
media/books/canva/
